<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home</title>
</head>
<body>
  <h1>Home</h1>
  <h2>Messages</h2>

  <% data.forEach(row => { %>
    <div class="message">
      <h2 class="title">
        <%= anon ? row.title : "Anon" %>
      </h2>
      <p class="text"><%= row.body %></p>
      <p class="timestamp"><%= row.timestamp %></p>
    </div>
  <% }) %>

  <% if (!user) { %>
    <p>Login to insert message</p>
  <% } else { %>
    <form method="post" action="/addmsg">
      <label for="title">Title</label>
      <input type="text" name="title" id="title" placeholder="title" required />

      <label for="msg">Message</label>
      <input type="text" name="msg" id="msg" placeholder="Your Message" required />

      <button type="submit">Submit</button>
    </form>

    <form method="get" action="/log-out">
      <button type="submit">Log out</button>
    </form>
  <% } %>
</body>
</html>
